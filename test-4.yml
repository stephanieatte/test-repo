steps:
  - label: "Pre Executess"
    key: "pre-execute"
    command: "echo hi"
 

  - label: "Get webhook"
    command: |
       curl https://graphql.buildkite.com/v1 \
          -H "Authorization: Bearer bkua_63fa9fec9d119f650e8b00a2b5f0c5c20036cc65" \
          -H "Content-Type: application/json" \
          -d '{
            "query": "query FindWebhookPayload { build(uuid: "0195d3de-724b-467a-a3f6-d561b2c52b6f") { source { ...on BuildSourceWebhook { headers payload } } } }",
            "variables": "{}"
          }'
notify:
  - slack: "@stephanie"
